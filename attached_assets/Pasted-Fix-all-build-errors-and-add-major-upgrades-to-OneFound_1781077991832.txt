Fix all build errors and add major upgrades to OneFounder. Do the following in order:
STEP 1 — Fix Vercel/npm build (critical)
In package.json, add an "overrides" field to force zod v3:
json"overrides": { "zod": "^3.25.76", "better-call": { "zod": "^3.25.76" } }
Also create server/tsconfig.json with this content (the build script needs it):
json{ "extends": "../tsconfig.json", "compilerOptions": { "outDir": "../dist/server", "rootDir": "." }, "include": ["./**/*.ts"] }
Delete node_modules and package-lock.json and reinstall. Run npm run build and confirm it passes.
STEP 2 — Add Claude AI as real provider (replaces mock)
Create server/ai/claude.ts implementing the AIProvider interface. Use fetch to call https://api.anthropic.com/v1/messages with claude-sonnet-4-20250514 model. Read ANTHROPIC_API_KEY from env. Update server/ai/index.ts to check for ANTHROPIC_API_KEY first, then fall back to Ollama, then fall back to mock. Show which provider is active on the Settings page.
STEP 3 — Add WordPress / Website Manager module
Create a new page client/src/pages/WordPressPage.tsx with these tabs: Connect (add WordPress site via URL + Application Password), Posts (list/create/edit/publish posts), Analytics (page views from WP stats), SEO (per-post score). Create server/routes/wordpress.ts with routes: POST /api/wordpress/sites, GET /api/wordpress/sites/:id/posts, POST /api/wordpress/sites/:id/posts, DELETE /api/wordpress/sites/:id. Add wpSites table to server/db/schema.ts with fields: id, userId, siteUrl, appPassword (store encrypted with basic base64 for now), siteName, active, createdAt. Register the route in server/index.ts. Add the page to App.tsx routing and add { id: 'wordpress', icon: '🌐', label: 'Website Manager' } to the Grow section in AppShell.tsx.
STEP 4 — AI Morning Briefing on Dashboard
In DashboardPage.tsx, add a "Good morning" card at the top that calls the existing /api/ceo/health-score endpoint. Use the data to generate 3 bullet insights (top opportunity, biggest problem, today's recommended action). Show as a styled card with a "Refresh" button. The text should be generated by the AI provider.
STEP 5 — Replace hash routing with React Router
Install react-router-dom. Refactor App.tsx to use <BrowserRouter> with <Routes> and real URL paths (/, /seo, /crm, /wordpress, etc.). Update AppShell.tsx navigation to use <Link> components. Update Vite dev config and Express server to serve the SPA correctly on all routes.
STEP 6 — Add Founder Profile to Settings
In SettingsPage.tsx, add a "Founder Profile" section where the user can set: risk tolerance (slider: conservative → aggressive), work style (builder/marketer/operator), primary goal (get first customer / reach $10k MRR / raise funding / grow team). Save to a new founderProfiles table. AI uses this context in all prompts to personalise responses.
After all steps: run npm run build and confirm zero errors.